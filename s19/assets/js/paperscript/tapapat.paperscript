// Create paper.js path to draw a line into it;
var path = new Path();
//pick color
path.strokeColor = 'white';

var start = new Point {100, 100};

path.moveTo(start);

path.lineTo(start + [100, -50]);
